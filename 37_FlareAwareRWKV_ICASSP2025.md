# Flare-Aware RWKV for Flare Removal

> **发表**：ICASSP 2025（2025 年 4 月 6–11 日，印度海得拉巴，pp. 1–5）　|　**作者**：Wanying Zhang, Wei Shang*, Dongwei Ren, Wangmeng Zuo（哈尔滨工业大学 Wangmeng Zuo 团队，Wei Shang 为通讯作者）
> **论文**：[IEEE Xplore 10888487](https://ieeexplore.ieee.org/document/10888487)（DOI: 10.1109/ICASSP49660.2025.10888487）　|　**代码**：未开源（截至 2026 年 6 月未检索到官方仓库，作者主页也未提供代码链接）

> ⚠️ **说明**：原文 PDF 不可公开获取（IEEE 付费墙，无 arXiv 预印本），本报告基于 Semantic Scholar / IEEE 页面摘要、IEEE 免费预览的引言片段、作者主页及公开资料撰写，模块细节与实验数字的置信度有限。论文配图无法获取，故本报告无图。

## 一、解决的问题

镜头光晕（lens flare）是强光源直射镜头时，光线在镜组内反射、散射产生的伪影，夜景拍摄中尤为常见，表现为散射状光纹（streak/glare）与反射鬼影（ghost）。光晕去除的目标是精确消除光晕区域并恢复被遮蔽的场景内容。论文引言指出，依赖硬件与拍摄技巧的直接方案灵活性和鲁棒性有限；学习类方法则依赖配对数据，代表性工作包括 Wu et al. 的半合成光晕数据集 + U-Net/pix2pix 恢复方案、Qiao et al. 基于循环一致性约束的非配对训练与光源感知引导框架，以及 Flare7K / Flare7K++ 这两个奠基性的夜间光晕数据集（合成 Flare7K 与实拍 Flare-R 的组合），Zhou et al. 则专门处理大小、形状、照度各异的多光源场景。

作者抓住的痛点是：**现有工作的重心几乎都放在数据合成与采集策略上，缺乏针对光晕去除任务专门设计的网络架构**。主流方法直接套用通用恢复骨干（U-Net、Uformer、Restormer 等）。而光晕是典型的大范围、跨区域的全局性退化（光纹可横贯整幅图像），用 CNN 建模感受野不足，用 Transformer 的全局自注意力则有二次方计算复杂度负担。本文于是引入 NLP 领域的 RWKV（Receptance Weighted Key Value）线性复杂度序列建模架构，试图在"全局依赖建模能力"与"计算效率"之间取得更优折中。

## 二、核心创新点

1. **首个面向光晕去除的 RWKV 架构**：将 RWKV 这种线性复杂度、可并行训练的类 RNN 架构引入光晕去除任务，以线性计算开销捕获全局依赖，填补该任务"专用架构"的空白（按论文自述）。
2. **轻量级光晕检测网络做前置引导（flare-aware）**：先用一个轻量检测网络定位光晕区域，再以检测结果引导后续恢复网络，使恢复过程具备"光晕感知"能力，而非对全图无差别处理。
3. **对 RWKV 两大核心机制的任务适配性分析**：系统分析了 RWKV 中注意力机制（WKV attention）与 token shift 机制对光晕去除任务的作用，并据此"精心挑选并以小幅调整方式集成"适配该任务的变体——这是论文方法部分的主要分析性贡献。
4. 据摘要，方法在多个数据集上表现良好，**尤其在真实场景（real-world）上有优势**。

## 三、模型结构与方案

由于原文不可得，以下为基于摘要、引言预览与 RWKV 视觉化相关公开文献的整理与推断，已标注推断成分。

**整体 pipeline（两阶段）**：输入光晕图像首先经过一个**轻量级光晕检测网络**，输出光晕区域的定位信息（推断为光晕掩码或显著图）；该先验随后注入**基于 RWKV 的恢复网络**，引导其对光晕区域做针对性恢复、对非光晕区域保持保真。这种"检测引导恢复"的设计与此前 Qiao et al. 的光源感知引导思路一脉相承，但把引导信号从光源位置扩展为显式的光晕区域检测。

**RWKV 恢复网络**：RWKV 原为 NLP 架构，其 block 由 time-mixing（含 WKV 注意力，即一种以指数衰减权重做键值聚合的线性注意力）与 channel-mixing 两部分组成，并用 token shift 将相邻 token 特征插值混合以增强局部建模。视觉化改造的通行做法（Vision-RWKV 的双向 Bi-WKV 注意力 + 四向 Q-Shift，Restore-RWKV 的递归 Re-WKV + 全向 Omni-Shift 等）是把图像展平为 patch 序列后做双向/多向扫描以消除一维因果偏置。本文摘要明确说作者**分析了注意力机制与 token shift 机制这两个关键模块对光晕任务的意义，并"精心挑选、小幅调整后集成"**——可合理推断其从上述视觉 RWKV 机制谱系中选型并做了任务化微调（例如让 token shift 的感受野适配光晕的大尺度连续结构），但具体选择了哪种 WKV 变体与 shift 方案、检测先验以何种方式（拼接/调制/注意力偏置）注入，无法从公开资料核实。

**损失函数与训练数据**：原文细节不可得。从引言对 Flare7K/Flare7K++ 的重点介绍及"多数据集、尤其真实场景表现好"的表述推断，训练应遵循该领域标准协议（Flare7K/Flare7K++ 合成配对数据训练，含光晕图 = 背景图 ⊕ 光晕图层），检测网络的监督信号推断来自合成数据自带的光晕掩码标注。此段均为推断，置信度中等。

（注：论文配图位于付费墙内且无任何公开镜像，本报告无法提供结构图。）

## 四、实验与结果

公开渠道仅能确认摘要中的定性结论：**方法在不同数据集上均有良好表现，且在真实场景上的优势最明显**。按领域惯例与引言内容推断，定量评测大概率在 Flare7K 与 Flare7K++ 的合成/真实测试集上进行，与 U-Net、Uformer 等基线比较 PSNR/SSIM/LPIPS（Flare7K++ 协议还含 G-PSNR、S-PSNR）；摘要还暗示对 RWKV 内部两机制（注意力、token shift）做了消融分析以支撑选型结论。**具体数值因原文不可得，本报告不予列出，以免编造。**

外部佐证方面：截至 2026 年 6 月，该文被引 3 次。其中 Semi-LAR（2026 预印本）在相关工作中将其概括为"以序列建模范式捕获长程光晕依赖、降低注意力开销"的代表；《A Survey of RWKV》与 AAAI 2026 的 SLCFormer 亦有引用，但均未给出可转引的对比数字。

## 五、专家锐评

**价值**：这是一篇典型的 ICASSP 4 页短文——把 RWKV 这一线性复杂度架构首次带进光晕去除，并配一个检测引导机制。其真正的贡献不在"发明"而在"选型论证"：光晕确实是大尺度全局退化，全局依赖 + 线性开销的卖点与任务性质对得上号；对 WKV 注意力与 token shift 两机制做任务适配性分析，比单纯堆模块的同类移植工作多了一层思考。在 2025 年 RWKV/Mamba 系架构涌入底层视觉的浪潮中（同届 ICASSP 即有 RWKV 去雨、RWKV 水下增强），它是光晕方向上占坑较早的一篇，被后续工作当作"线性注意力路线"的代表引用。

**不足与质疑**：
1. **动机陈述夸大**。"缺乏针对该任务的专用架构"这一立论站不住脚：Qiao et al. 的光源感知引导（ICCV 2021）、Zhou et al. 的多光源专用设计（2023）、Flare7K++ 配套的光源掩码后处理等都是任务专用设计。该文真正做的是换骨干，而非首创"专用架构"。
2. **架构新颖性边际**。摘要自述对 RWKV 机制只做"minor adjustments"后集成——Vision-RWKV、Restore-RWKV 已把 Bi-WKV/Re-WKV、Q-Shift/Omni-Shift 等部件备好，本文更像在既有菜单上做任务化拼装；"检测网络引导恢复"也是该领域反复出现的套路（光源/掩码引导）。两项主要设计的增量都偏薄。
3. **检测先验的病态性与级联误差**。光晕是半透明、无清晰边界的退化，"光晕掩码"的真值本身定义模糊，且只能来自合成数据的图层标注；检测网络一旦在真实图上失准（夜景中光晕与路灯、霓虹的辨识本就困难），错误先验会被直接喂给恢复网络，两阶段设计的误差传播风险论文（从摘要看）未见讨论。
4. **效率主张缺乏可见证据**。线性复杂度是核心卖点，但该领域标准测试分辨率仅 512×512，在此尺度下 Uformer/Restormer 的窗口/转置注意力开销并不构成瓶颈；没有公开的 FLOPs/延迟对比，"高效"停留在渐近复杂度层面。
5. **可复现性差**。无代码、无预印本、无项目页，4 页篇幅也难容纳完整实现细节；社区无法验证其"真实场景优势"，3 次引用中无一篇做过定量复现或对比，这与同期开源工作（如 Flare7K++ 系）形成鲜明反差。
6. **真实泛化的证据面窄**。该领域所谓"real-world 评测"通常即 Flare7K++ 的 100 张真实测试图 + 定性图，样本量小且光源类型有限；在合成数据上训练的检测 + 恢复两个网络，其真实域优势有多稳健，仅凭摘要的一句"particularly on real-world scenarios"难以采信。

总体而言：选题切口合理、踩点及时的一篇短文，但公开可验证的信息过少，其声称的优势目前只能"姑且听之"。若后续开源并补充高分辨率效率对比与真实域大样本评测，价值会更扎实。
