# A self-prompt based dual-domain network for nighttime flare removal

> **发表**：Engineering Applications of Artificial Intelligence (EAAI), Vol. 144, 2025（注：见文末"异常说明"——本文的权威检索结果指向 EAAI 而非 Neurocomputing）　|　**作者**：Kejing Qi, Bo Wang, Yun Liu
> **论文**：https://doi.org/10.1016/j.engappai.2025.110103 （ScienceDirect PII: S0952197625001034）　|　**代码**：未找到公开仓库

> 说明：原文 PDF 不可公开获取（付费墙，ScienceDirect/ResearchGate 均返回 403，Semantic Scholar 与 Crossref 均无摘要全文），亦未检索到预印本或官方代码仓库。本报告基于 Semantic Scholar / Crossref 元数据、出版商摘要页的多次网络检索片段及同领域公开资料综合撰写，方法细节（尤其是具体模块命名、损失函数、完整指标数字）置信度有限，凡无法溯源的数字一律未写入。

## 一、解决的问题

夜间镜头眩光（nighttime lens flare）由强点光源（路灯、车灯、霓虹）在镜头内部多次反射、折射以及传感器阵列散射产生，表现为光晕（glow / veiling glare）、条纹、鬼影反射（reflective flare）、星芒等多种形态。它会大面积污染夜景照片，破坏纹理与对比度，并对夜间感知类下游任务（自动驾驶、安防监控）造成干扰。这是近两年随 Flare7K / Flare7K++ 数据集出现而被系统研究的一个细分复原任务。

本文指出的核心痛点是：**现有方法大多把眩光当作"单一类型、纯空间域"的退化来处理**。但真实复杂夜景中，眩光是由多种退化因子叠加而成的——不同类型的眩光（散射型、反射型、光晕型）在空间分布、频率特性上差异很大，且常与背景纹理高度混淆，难以区分。用单一空间域网络一刀切地回归"干净图"，既难以精准定位眩光区域，也难以针对不同眩光类型施加差异化处理，往往导致过度平滑（连真实纹理一起抹掉）或残留。

为此，作者提出从两个角度突破：其一，引入**频率域**信息与空间域协同（dual-domain），用频域特征捕捉眩光这种大范围、低频为主又含周期性条纹的全局退化；其二，引入**自提示（self-prompt）机制**，让网络自动编码不同眩光类型的数据分布、生成"提示特征"，再把提示注入解码器，从而对不同眩光类型给出有针对性的去除引导。

## 二、核心创新点

1. **自提示（Self-Prompt）模块**：网络自行编码不同眩光类型的数据分布，生成 prompt features，并将这些提示特征与解码器交互，引导网络识别并区分不同类型的眩光。区别于人工提示或文本提示，这里的提示是从数据中自学习得到的，目的是缓解"多类型眩光难以用单一映射处理"的问题。

2. **双域（Dual-Domain）特征提取**：在传统卷积网络中引入快速傅里叶变换（FFT）以提取全局频率特征，捕捉眩光的全局/低频结构；同时保留空间域分支处理局部纹理。空间域与频率域协同感知眩光区域。

3. **并行注意力（Parallel Attention）**：在网络中加入并行注意力，用于提取依赖位置的局部信息（location-dependent local information），帮助网络更准确地定位眩光区域，配合频域分支构成"全局频率 + 局部空间"的互补结构。

4. **提示特征与解码器的交互式注入**：提示特征不是简单拼接，而是据检索片段描述通过多个中间组件与（疑似）反向投影（back-projection）方式与解码器交互，起到逐级引导去眩光的作用（此条细节来自检索片段，置信度有限）。

## 三、模型结构与方案

整体上，方法（下文按检索资料暂称 SPDDNet 风格的双域去眩光网络）采用编码器-解码器（U 形）骨架，在其上叠加两条关键设计线索：

- **双域编码**：编码阶段，除常规卷积空间分支外，引入 FFT 分支，把特征变换到频率域以提取全局频率特征，再变换回空间域与卷积特征融合。频率域有利于建模眩光这类跨越大片区域、以低频光晕和周期性条纹为主的退化，而单纯堆叠空间卷积感受野有限、难以一次性"看全"整片眩光。

- **并行注意力**：在编码/瓶颈处加入并行注意力，提取与位置相关的局部信息，强化对眩光区域边界、强光源附近高响应区的感知，与频域全局特征形成互补。

- **自提示与解码引导**：网络通过一个提示生成单元，编码不同眩光类型的分布信息，产出一组 prompt features；解码阶段，这些提示特征与解码器特征交互（据片段含多级中间组件与 back-projection），相当于给解码器"看到当前要处理哪一类眩光"的先验，从而对散射型、反射型、光晕型等施加差异化的恢复策略。

- **训练数据/合成策略**：与该领域主流一致，训练采用 Flare7K / Flare7K++ 体系下的合成数据——把清晰背景图与从 Flare7K（5000 张散射眩光 + 2000 张反射眩光）及 Flare-R（962 张真实眩光）中采样的眩光层及对应光源进行合成（检索片段提到训练时以约 50% 概率随机选取眩光图与光源）。这些数字属于 Flare7K++ 数据集的公开规格，并非本文新增。

> 注：本报告未能获取原文网络结构图（论文付费墙、无开源代码仓库、无预印本），因此本节不嵌入结构图，以免塞入无关示意图误导读者。模块的精确连接方式（FFT 分支放置位置、提示注入的具体算子、back-projection 的实现）需以原文 Fig. 为准。

## 四、实验与结果

根据公开检索片段，实验在夜间眩光去除标准基准 **Flare7K / Flare7K++**（含 962 张真实测试眩光场景）上进行，评价指标沿用该领域惯例：PSNR、SSIM、LPIPS，并通常单独报告"光晕区域 (Glare)"与"全图 (Image)"两套指标。论文与 Uformer、MPRNet、Restormer、HINet 以及 Flare7K 官方基线等方法做对比，并对自提示模块、FFT 频域分支、并行注意力分别做消融以验证各组件贡献。

**需要明确说明**：由于原文付费墙、无法读到正文与表格，本报告无法核实任何具体的 PSNR / SSIM / LPIPS 数值。为遵守"数字必须溯源、不得编造"的要求，此处**不给出任何具体指标数字**。Semantic Scholar 记录该文截至检索时被引 3 次。读者如需精确数字，请以 ScienceDirect 原文 Table 为准。

## 五、专家锐评

**价值**：在 2024–2025 年这一波夜间去眩光研究里，本文抓住了一个真实存在的薄弱点——"把眩光当单一空间域退化"确实是早期 Flare7K 基线的局限。它把两条当下流行的思路（频域/双域处理 + prompt 学习）落到去眩光这个具体任务上，自提示按眩光类型生成引导，配合 FFT 全局频域分支，方向上是合理且与任务物理特性吻合的（眩光本就是大范围、含周期条纹的退化，频域确有优势）。作为期刊工程应用类工作，它的定位是"把成熟组件组合到位、刷高基准"，这类工作有其工程价值。

**不足与质疑**（资深审稿人视角，至少 3 条）：

1. **新颖性高度边际，组件均为"拿来主义"**。FFT 双域分支在图像复原里早已被 FFC、DeepRFT、以及同期去眩光工作（如频域引导的 DFDNet 思路）反复使用；prompt 学习也由 PromptIR 等开创。本文把"频域 + 并行注意力 + prompt"三件套拼到去眩光，几乎每一件单看都不新，真正的原创性在于"自提示如何按眩光类型编码分布"这一点，但摘要层面看不出它与 PromptIR 式可学习 prompt 的本质区别，作者需要论证为何它专门适配"多类型眩光"而非通用复原 prompt。

2. **"区分多种眩光类型"的核心卖点缺乏可验证证据**。论文立论建立在"自提示能区分散射/反射/光晕等不同类型"之上，但这是一个很强的主张。理想情况下应给出提示特征在不同眩光类型上的聚类/可视化、或按类型分组的去除效果分解；仅靠端到端 PSNR 提升无法证明"提示真的学到了类型语义"，存在把性能增益简单归因于参数量或频域分支的风险。可惜付费墙下无法核验其是否做了此类分析。

3. **评测仍重度依赖 Flare7K 合成数据，泛化性存疑**。Flare7K++ 的眩光层是半合成、半真实采集，训练以约 50% 概率合成的范式会让模型对"合成眩光统计"过拟合，而真实夜景中的复杂光源（动态车灯、湿地面反光、HDR 过曝）分布差异很大。论文若仅在 962 张真实测试集上报告指标，仍属同源评测，跨数据集/真实手机夜拍的泛化几乎没有被检验——这是整条技术路线（包括很多同行工作）的共性短板，本文未见突破迹象。

4. **效率与工程落地未见交代**。FFT 分支 + 并行注意力 + 多级 prompt 注入都会带来不小的计算与显存开销，而本文发表在"工程应用"导向的期刊，理应给出参数量、FLOPs、推理延迟，并讨论在边缘设备（车载/手机 ISP）上的可行性。从可获取信息看不到这部分讨论，"工程应用"的成色打折扣。

5. **元数据与可复现性问题**。该文无公开代码、无预印本，且本任务给出的 venue（Neurocomputing）与 PII（S0925231225007726）与权威检索（Crossref/Semantic Scholar 一致指向 EAAI, DOI 10.1016/j.engappai.2025.110103）不符，增加了溯源与复现难度。对一篇主打"刷基准"的工作而言，不开源会显著削弱其影响力与可信度。

---

**异常说明**：
- 本任务标注 venue=Neurocomputing、链接 PII=S0925231225007726，但 Crossref 与 Semantic Scholar 一致显示该标题论文发表于 **Engineering Applications of Artificial Intelligence (EAAI), Vol. 144, 2025, DOI 10.1016/j.engappai.2025.110103, PII S0952197625001034**。即"README 误标 EAAI"的提示与权威数据库相反——按数据库，EAAI 才是正确出处。报告标题区已据此标注，请人工复核。
- 原文付费墙，未获取到 PDF、摘要全文、网络结构图，亦无开源代码/预印本。故报告未嵌入任何图（无可靠图可用，按指南不硬塞无关图）。具体指标数字因无法溯源未写入。
