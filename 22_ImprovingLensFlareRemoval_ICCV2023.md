# Improving Lens Flare Removal with General-Purpose Pipeline and Multiple Light Sources Recovery

> **发表**：ICCV 2023　|　**作者**：Yuyan Zhou et al.（南京航空航天大学，合作方含 DJI、南开大学）
> **论文**：ICCV 2023 Open Access（本仓库 pdfs/22_ImprovingLensFlareRemoval_ICCV2023.pdf）　|　**代码**：https://github.com/YuyanZhou1/Improving-Lens-Flare-Removal

## 一、解决的问题

眩光去除的训练数据几乎全靠合成：Wu et al.（ICCV 2021）与 Flare7K 都假设"眩光层与场景层在 RAW 空间可加"，于是把 RGB 图做逆 gamma 当作 RAW，直接相加再 clip。本文系统性地指出这条直接相加（direct-add）管线忽略了 ISP（图像信号处理管线）中的两个关键环节：

其一，**色调映射（TMO）**。真实 ISP 先用非线性 TMO 把 HDR 照度压到 LDR，再做 gamma 校正；逆 gamma 并不能恢复 RAW。对场景层而言多数像素落在 TMO 的线性段，线性近似尚可；但眩光层光源附近的大量像素落在 TMO 的肩部（Shoulder section），按线性处理会让合成图在光源附近对比度和色彩被"压扁"，出现低对比的过饱和（图 2a）。

其二，**自动曝光（AE）**。强光源入画时，相机会自动缩短快门/缩小光圈，导致背景整体变暗、动态范围变窄（作者用 iPhone 13 Pro 实测 100 张：无/有强光源的平均快门分别为 9.85×10⁻⁴s 和 7.52×10⁻⁵s）。直接相加却让合成图整体变亮，与真实分布相反，造成训练数据的分布偏移，模型在真实图上泛化差。

此外，**光源恢复**是另一个老大难：网络往往把光源连同眩光一起抹掉，Wu/Dai 的补救是用亮度硬阈值（如 0.99）找最亮连通域再贴回。这套阈值法在白天/夜间光源照度差异巨大、以及多光源场景下频繁失效——只能恢复最亮的那个光源，月亮、路灯等较暗光源直接丢失。

## 二、核心创新点

1. **理论分析驱动的凸组合合成管线**：以平滑阶跃 TMO T(x)=3x²−2x³ 为分析工具，推导出"RAW 空间相加"在 RGB 空间的等效形式近似为**逐像素凸组合**——眩光像素越亮权重越大（趋近 1），越暗权重越小（趋近 0），从理论上解释了为何凸组合优于直接相加，并同时模拟了 AE 的全局压暗效应，避免分布偏移与过饱和。
2. **无硬阈值的多光源恢复策略**：用强凸权重函数 W_r = ((I−min)/(max−min))^α（α=15）对网络输入与输出做逐像素凸平均 I_final = (1−W_r)⊙N(C) + W_r⊙C。min-max 归一化保证最亮处权重恒为 1，强凸性把权重集中在 TMO 肩部像素（即光源），从而一次性恢复任意数量、形状、亮度的光源，无需阈值与连通域后处理。
3. **消费电子泛化测试集**：100 张真实眩光图，覆盖 10 种设备（iPhone 13 Pro / iPhone 11 / 小米 12S Ultra / iPad Air4 / 华为 Mate 40 等），日间 + 夜间、太阳/月亮/路灯/闪光灯等多种光源，streak/spot/blob/haze/色彩溢出等比现有测试集更丰富的眩光形态，专门用于检验泛化能力。
4. **方案与网络解耦**：所有改进都在数据合成与前后处理层面，可与任意修复网络（U-Net、Uformer）组合，是"通用管线"而非新网络。

## 三、模型结构与方案

**合成管线**三步走：(1) 计算眩光层照度矩阵 I_F = (1/(255×3))·Σ_{c∈{r,g,b}} F_c；(2) 用 sigmoid 形权重函数 f(x)=1/(1+e^{p(x−q)})（q=0.5，p∼U[4,7]，每图随机采样）由照度生成权重矩阵 W = f(I_F)；(3) 在逆 gamma 空间做凸组合 I = (1−W)⊙S + W⊙F + N(0,σ²)，σ²∼0.01χ² 与前作一致。直观上：眩光亮处场景被遮蔽（W→1），眩光暗处对成像几乎无贡献（W→0），全图无溢出、无 clip。

![ISP 与 TMO](images/22_ImprovingLensFlareRemoval_ICCV2023/fig3_p4.png)
> 图 3：简化 ISP 流程（RAW → Tone Mapping → Gamma 校正 → RGB）与两种 TMO 曲线，光源像素落在肩部、场景像素落在线性段，是全文理论分析的出发点（来源：原论文）

![合成与光源恢复对比](images/22_ImprovingLensFlareRemoval_ICCV2023/fig2_p3.png)
> 图 2：(a) 直接相加 vs (b) 本文凸组合合成——避免过饱和低对比；(c) 阈值法 vs (d) 本文光源恢复——较暗光源也能保留（来源：原论文）

**光源恢复**：推理时对真实眩光图 C 计算照度矩阵并 min-max 归一化，取 α 次幂得 W_r，再与网络输出 N(C) 凸混合。α→+∞ 时只恢复光源，α 过小则眩光也被混回；实验表明 α≥15 后 PSNR/SSIM 稳定（17.88/0.528），故默认 α=15。

**训练设置**：以 U-Net 为基线（与 Wu et al. 对齐），另测 Uformer；分别用 Wu 的眩光数据集与 Flare7K 在相同背景图集上合成训练数据；TensorFlow 实现，单卡 GTX 3090，另提供 MindSpore 实现。

## 四、实验与结果

**定量**（Flare7K 测试集，含日间+夜间成对数据）：Ours(Wu 数据集)+U-Net 达 PSNR 25.9 / SSIM 0.896，显著优于 Wu+U-Net 的 23.6/0.870（+2.3 dB）；Ours(Wu)+Uformer 26.3/0.884 为全表最高 PSNR。用 Flare7K 训练时本文为 25.3（U-Net）/25.7（Uformer），与 Dai 的 25.4/25.7 基本持平——作者解释 Flare7K 眩光全为合成图，受 AE/TMO 失真影响小。

**定性与用户研究**：传统方法（Asha et al.）、去反射（Zhang et al.）、去雾（He et al.）均无法彻底去眩光；Wu 法夜间几乎失效，Dai 法日间偏弱；本文在日夜两端都更干净。用户研究中本文相对各对比方法的得票率多在 5 成到 10 成（如 vs Wu 在 [6] 测试集 100%:0%）。

**光源恢复**：单光源——夜间月亮、路灯亮度低于 0.99 阈值，Wu/Dai 法无法恢复而本文可以；多光源——阈值法只能恢复最亮光源，本文恢复全部（图 7）。**下游任务**：YOLOv5 在去眩光后能检出原先被条纹遮蔽的椅子/摩托车，且不再把反射眩光误检为汽车/红绿灯。

![多光源恢复](images/22_ImprovingLensFlareRemoval_ICCV2023/fig7_p8.png)
> 图 7：多光源恢复——阈值法 [31,6] 只能恢复最亮光源，本文方法恢复了背景中所有大小、位置不同的光源（来源：原论文）

## 五、专家锐评

**价值**：这是眩光合成管线从"经验叠加"走向"ISP 物理建模"的关键一篇。它第一次把 TMO 肩部非线性与 AE 全局压暗这两个被默认忽略的 ISP 环节摆上台面，并给出可执行的逐像素凸组合替代方案；其影响是实打实的——后续 Harmonizing Light and Darkness（arXiv 2024）等工作直接把"Zhou et al. 的凸组合"当作合成基线引用与比较。光源恢复用幂函数软权重替代硬阈值 + 连通域，优雅地统一了单/多光源、明/暗光源的处理，至今仍是免训练光源贴回方案里最干净的设计之一。消费电子测试集补上了"跨设备泛化"这一评测空白。

**不足与质疑**：

1. **理论与实现之间有一道裁缝缝**。推导基于特定的 smoothstep TMO 和 ε 高阶小量近似，结论只是"权重随眩光亮度单调"这一定性形态；最终落地的却是 q=0.5 固定、p 在 [4,7] 随机采样的 sigmoid——这两个超参数与任何相机的真实 TMO 都没有标定关系，p 的取值范围如何确定、对结果多敏感，论文没有任何消融。所谓"理论分析"实际只起到动机作用，宣称的严格性高于实际成色。
2. **自家结果暴露了增益的条件性**：用 Flare7K 训练时，本文管线 U-Net 25.3 还略低于 Dai 的 25.4，Uformer 则与 Dai 持平（25.7），即对纯合成眩光数据**新管线几乎无增益甚至轻微负增益**；+2.3 dB 的亮眼数字只在 Wu 的实拍眩光数据集上成立。"general-purpose" 的标题与这一对数据依赖的事实之间存在张力，作者虽给了解释，但未做进一步实验验证（例如对 Flare7K 眩光做 AE 仿真预处理）。Uformer 上 PSNR 升 SSIM 降（0.884 < U-Net 的 0.896）的反常现象也未解释。
3. **光源恢复的固有缺陷被轻描淡写**：W_r 只看亮度，无法区分"光源本体"与"同样落在 TMO 肩部的眩光亮核"（饱和的 streak 中心、光源周围的 glare 晕），α=15 的全局幂函数必然把这部分眩光一起混回输出；α 消融的 PSNR 绝对值只有 17.88，侧面说明该子任务的恢复质量整体偏低，但论文未讨论失败案例。
4. **评测短板**：全文只有 PSNR/SSIM，没有 LPIPS 等感知指标（而本文卖点之一恰是视觉自然度）；用户研究未报告参与人数与统计显著性；消费电子测试集无配对 GT，只能做主观评价，作为"泛化基准"的可量化价值有限；AE 实测只覆盖 iPhone 13 Pro 一种机型 100 张，样本面偏窄。
