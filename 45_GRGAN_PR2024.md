# GR-GAN: A Unified Adversarial Framework for Single Image Glare Removal and Denoising

> **发表**：Pattern Recognition 2024（Vol. 156, Article 110815，2024 年 12 月卷）　|　**作者**：Cong Niu, Ke Li, Di Wang, Wenxuan Zhu, Haojie Xu, Jinhui Dong（西安电子科技大学，合作单位含 University of Strathclyde）
> **论文**：https://www.sciencedirect.com/science/article/pii/S0031320324005661（DOI: 10.1016/j.patcog.2024.110815）　|　**代码**：未开源（未检索到官方仓库）

> ⚠️ **原文 PDF 不可公开获取（Elsevier 付费墙，无 arXiv 预印本），本报告基于摘要、出版商页面检索摘录、ADS/Semantic Scholar/OpenAlex 元数据及公开资料撰写，细节置信度有限。** 论文配图无法合法获取，且未找到官方代码仓库可供截图，本报告无插图。

## 一、解决的问题

本文针对的是**单图眩光去除（Single Image Glare Removal, SIGR）**：在强光照场景下，外部光源造成的眩光（glare spots、光斑、光线条纹）叠加在图像上，破坏内容可见性。作者将 SIGR 作为一个（他们声称的）较少被系统研究的任务提出，并强调真实眩光图像的两大难点：**眩光形状不规则**（点状、芒线状、片状混杂）以及**背景杂乱**，使得固定感受野的常规卷积难以贴合眩光的几何形态。

第二个痛点是任务耦合：低光/夜间拍摄下眩光往往与噪声同时出现，分两个模型先去噪再去眩光（或反之）会产生误差累积。因此作者主张用**一个统一的对抗框架同时处理眩光去除与去噪**，这也是标题中 "unified ... glare removal and denoising" 的含义。

第三个痛点是数据：此前缺少专门面向 SIGR 的成对基准，作者因此自建了 **De-Glare** 数据集，包含真实与合成的 {glare, glare-free} 图像对。据公开检索摘录，规模约为 **1000 对真实图像 + 2000 对合成图像**（其中约 800 对真实 + 1600 对合成用于训练），数字以原文为准。

## 二、核心创新点

1. **提出 SIGR 任务设定与 De-Glare 基准**：真实+合成混合的成对眩光数据集，为该任务提供训练与评测基础（真实对约千对量级）。
2. **GAD（Glare Attention Detector，眩光注意力检测器）**：基于**可变形卷积（deformable convolution）**生成注意力图，显式定位输入图像中的眩光光斑与光线条纹——用可变形采样点去适配眩光的不规则几何形状，是其针对眩光形态特性的核心设计。
3. **CPN（Cascaded Pyramid Neck，级联金字塔颈部）生成器**：多尺度级联结构，聚合多尺度视觉特征，自适应滤除无关噪声、增强显著特征，使去眩光与去噪在同一生成器内统一完成。
4. **统一对抗框架**：整体为多尺度 GAN（生成器 + 判别器），以对抗训练保证修复区域的纹理真实感；据公开资料，框架还被迁移到叠加图像分离（superimposed image separation）、去雨、去阴影等任务上验证通用性。

## 三、模型结构与方案

基于可获取的公开信息，整体 pipeline 可以概括为：

- **生成器**：输入眩光图像，先由 GAD 以可变形卷积预测眩光注意力图（标注眩光斑点/光线的空间位置与形状），注意力图与多尺度特征一同送入 CPN；CPN 以级联金字塔的方式在多个分辨率上提取并融合特征——低分辨率层负责大面积光雾/光晕的全局抑制，高分辨率层负责光斑边缘与背景纹理的精细重建；注意力机制同时起到"只动眩光区域、少动干净背景"的门控作用，并在同一前向过程中抑制噪声。
- **判别器**：对生成结果做真假判别，迫使修复区域纹理与真实 glare-free 图像分布一致（多尺度判别的具体形式以原文为准）。
- **损失函数**：对抗损失 + 像素级重建损失的组合是此类框架的标准配置；原文具体损失项与权重因付费墙无法核实，此处不展开。
- **训练数据**：De-Glare 的真实对 + 合成对混合训练；合成对的生成方式（眩光素材叠加策略）无法从公开资料确认。

公开摘录还显示，论文在四类任务上做了评估：图像眩光去除、叠加图像分离、图像去雨、图像去阴影——说明作者将 GR-GAN 定位为一个泛化的"减性退化分离"框架，而不仅是眩光专用模型。

## 四、实验与结果

因原文不可获取，**具体 PSNR/SSIM 数字无法核实，本报告不引用任何指标数值**。可确认的实验设定信息：

- **数据集**：自建 De-Glare（真实+合成）；另在去雨、去阴影、叠加图像分离的公开基准上做了扩展验证（具体基准名称待原文确认）。
- **对比对象**：据摘要措辞为"与代表性图像修复/增强方法相比取得有竞争力（competitive）的性能"。值得注意的是，摘要用词是 *competitive* 而非 *state-of-the-art*。
- **消融方向**：从结构设计可推断围绕 GAD（可变形注意力）与 CPN（多尺度级联）的有效性展开，具体数字不可得。
- **引用情况**：截至 2026 年中，Semantic Scholar 收录引用 13 次，引用者多来自图像分离/去噪/去雾化等邻近方向，而非主流 flare removal（Flare7K 系）社区。

## 五、专家锐评

**价值**：这篇文章的实际贡献是把"眩光"作为一种空间不规则的加性退化，用可变形卷积注意力 + 多尺度 GAN 给出了一个工程上自洽的解法，并补了一个真实成对的小规模基准。GAD 用可变形采样适配光芒/光斑几何形状，是针对眩光形态做的、有明确动机的设计点；"去眩光与去噪统一建模"也符合夜景实拍的真实退化组合。在 Pattern Recognition 的论文谱系里，这是一篇规规矩矩的"任务+数据集+定制网络"工作。

**不足与质疑**：
1. **"新任务"的说法站不住脚，且与主流 flare removal 文献脱节**。单图去眩光/炫光绝非空白：Wu et al.（ICCV 2021）、Qiao et al.（ICCV 2021）、Flare7K（NeurIPS 2022）、Flare7K++（TPAMI 2024）已构成完整的 benchmark 体系，glare 与 flare 在物理成因上高度重叠。从可见的公开信息看，论文未与 Flare7K 系预训练模型（如 Uformer 基线）在公认基准上对比，而是在自建 De-Glare 上声称 competitive——把任务重命名后绕开最强基线，这是审稿中应被直接质询的点。
2. **2024 年的纯 GAN 框架在方法论上已显陈旧**。同期 flare/glare 去除的 SOTA 已转向 Transformer（Uformer/Restormer 系）与扩散模型，GAN 的训练不稳定性与纹理幻觉问题众所周知；摘要自己只敢说 competitive，侧面印证了性能并不突出。所谓"统一去噪"亦无公开证据表明优于"强修复网络顺带去噪"的平凡方案。
3. **De-Glare 数据集规模小且采集协议不明**。约 1000 对真实图像的量级低于 Flare7K++ 的真实子集，更关键的是：真实场景下 {glare, glare-free} 成对图像如何对齐采集（遮挡光源？双次曝光？配准误差如何控制）公开资料完全查不到；合成对的眩光合成模型若只是简单线性叠加，则与真实眩光的非线性 ISP 耦合（Zhou et al. ICCV 2023 指出的问题）相悖。数据与代码均未开源，复现与横向比较都无从谈起。
4. **多任务"通用性"实验冲淡而非加强了贡献**。把同一网络拿去做去雨、去阴影、图像分离并报告不错的结果，恰恰说明 GAD/CPN 并没有多少眩光特异性——它更像一个通用的"空间注意力 + 多尺度 GAN"修复器；这与论文以 SIGR 为核心卖点的叙事自相矛盾。
5. **影响力与社区采纳度低**：发表一年余引用 13 次且几乎不被 flare removal 主线工作引用，De-Glare 也未成为任何后续论文的评测基准，说明该基准未获社区认可。

（注：以上对方法细节的描述均经多个独立检索源交叉印证，但受付费墙限制，模块内部结构与实验数字的置信度有限，请以原文为准。）
